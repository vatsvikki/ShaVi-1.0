There are two way to execute this inversion code to reprodue the model parameters:
First: To access the GUI of this code execute the Shavi-1.0.py scriot which is placed in bin file. There is a turorial link for its execution in help section which can make its implimentation easy.

Second: Another option is to directly access the code by using the executable (fwi.exe) placed in bin file. It will require the necessary file (parameter, source information, acquisition geometry,
and obsrve data)

A folder is also provided with the name synthetic data gerator whihc can help to produce the synthetic data for synthtic models.

